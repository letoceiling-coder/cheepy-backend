# Deploy to server: /var/www/online-parser.siteaacess.store/.env.testing
# Uses dedicated DB (not SQLite). Overrides production DB_DATABASE only.
APP_ENV=testing
DB_CONNECTION=mysql
DB_HOST=localhost
DB_DATABASE=online_parser_siteaacess_testing
